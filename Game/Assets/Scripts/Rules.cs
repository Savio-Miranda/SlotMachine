using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// FAZENDO MATRIZ DE GAME_OBJECTS PARA ADICIONAR REGRAS DE GANHO EM LINHAS A PARTIR DA ORDEM DAS SEQUÊNCIAS
public class Rules : MonoBehaviour
{
    //Gameobject[,] matrix = new int[3, 5];
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
